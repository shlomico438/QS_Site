{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run siteapp (Production)",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/siteapp.py",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "envFile": "${workspaceFolder}/.env",
            "env": {
                "SIMULATION_MODE": "0",
                "GEVENT_SUPPORT": "True"
            }
        }
    ]
}